= Spring AI RAG Demo
:linkattrs:
:project-owner:   megadotnet
:project-name:    spring-ai-redis-demo
:project-group:   com.redis.demo
:project-version: 0.0.1-SNAPSHOT
:project-title:   Spring AI RAG Demo

---

基于 link:https://docs.spring.io/spring-ai/reference/[Spring AI] 和 link:https://milvus.io/[Milvus] 向量数据库的检索增强生成（RAG）演示应用，集成 Rerank 精排功能。

== 技术架构

[source]
----
                    ┌─────────────┐
                    │   React UI  │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │ Spring Boot │
                    │   3.2.3     │
                    └──────┬──────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
┌───────▼───────┐  ┌───────▼───────┐  ┌───────▼───────┐
│  Ollama LLM   │  │    Milvus     │  │  SiliconFlow  │
│  (Chat/Embed) │  │ Vector Store  │  │   (Rerank)    │
└───────────────┘  └───────────────┘  └───────────────┘
----

=== 核心技术栈

[cols="1,2,2"]
|===
| 组件 | 技术 | 说明

| 后端框架
| Spring Boot 3.2.3 + Spring AI 1.0.0
| Java 17+

| LLM 服务
| Ollama (qwen2.5:0.5b)
| 本地部署，支持中文

| Embedding
| Ollama (bge-m3:latest)
| 1024 维向量

| 向量数据库
| Milvus
| 高性能向量检索

| Rerank 精排
| 硅基流动 API (BAAI/bge-reranker-v2-m3)
| 提升检索相关性

| 前端
| React 18 + Bootstrap 5
| 聊天界面
|===

== 前置条件

=== Docker

运行 Milvus 向量数据库：
[source,console]
----
docker compose up -d
----

=== Ollama

安装并运行 link:https://ollama.com/[Ollama]，下载所需模型：
[source,console]
----
ollama pull qwen2.5:0.5b
ollama pull bge-m3:latest
----

=== 硅基流动 API Key（可选，用于 Rerank）

从 link:https://cloud.siliconflow.cn/[硅基流动控制台] 获取 API Key。

== 配置

=== 环境变量

[source,console]
----
# Rerank 功能 (可选)
export SILICONFLOW_KEY=<你的硅基流动API Key>
----

=== 应用配置 (application.properties)

[source,properties]
----
# --- 模型提供者配置 ---
# 可选值: ollama, openai
# 默认使用 ollama
chat.provider=openai

# --- Ollama 配置 (当 chat.provider=ollama 时生效) ---
spring.ai.ollama.base-url=http://127.0.0.1:11434
# Embedding 模型
spring.ai.ollama.embedding.options.model=bge-m3:latest
# Chat 模型
spring.ai.ollama.chat.options.model=qwen2.5:0.5b

# --- OpenAI 配置 (当 chat.provider=openai 时生效) ---
# 推荐使用环境变量 SILICONFLOW_KEY 设置 API Key
spring.ai.openai.api-key=${SILICONFLOW_KEY:}
spring.ai.openai.base-url=https://api.siliconflow.cn
spring.ai.openai.chat.model=Qwen/Qwen3-8B

# --- Milvus 向量库配置 ---
spring.ai.vectorstore.milvus.client.host=10.1.1.234
spring.ai.vectorstore.milvus.client.port=19530
spring.ai.vectorstore.milvus.collection-name=paper_collection_vpdf

# --- RAG 检索配置 ---
# 初筛召回数量
topk=10
# Rerank 精排开关
rerank.enabled=true
rerank.model=BAAI/bge-reranker-v2-m3
rerank.topN=5

# --- 文档处理与切块配置 ---
# 切块算法: markdown (默认, 基于语义) 或 token (基于长度)
chunking.algorithm=markdown

# Markdown 切块参数
chunking.markdown.chunkTokenNum=4096
# 语义分割符
chunking.markdown.delimiter=\n!?;。；！？
# 表格单独切块
chunking.markdown.separateTables=true

# --- DeepSeek-OCR 配置 ---
# 启用 DeepSeek OCR 处理 PDF
deepseek-ocr.enabled=true
deepseek-ocr.model=deepseek-ai/DeepSeek-OCR
----

== 运行

[source,console]
----
git clone https://gitee.com/{project-owner}/{project-name}.git
cd {project-name}
mvn spring-boot:run
----

应用启动后，访问 link:http://localhost:8080[http://localhost:8080]。

== RAG 工作流程

[source]
----
1. 用户提问
      │
2. 向量召回 (Milvus TopK=10)
      │
3. Rerank 精排 (SiliconFlow TopN=5) [可选]
      │
4. 构建 Prompt + 上下文文档
      │
5. LLM 生成回答 (Ollama)
      │
6. 返回结果
----

== 项目结构

[source]
----
src/main/java/com/redis/demo/spring/ai/
├── RagApplication.java          # 应用入口
├── RagController.java           # REST API 控制器
├── RagConfiguration.java        # Spring 配置
├── RagDataLoader.java           # 数据初始化加载器
└── service/
    ├── RagService.java          # RAG 核心服务
    ├── RerankService.java       # Rerank 精排服务
    └── RerankResult.java        # Rerank 结果封装
----

== 相关文档

* link:Architecture.md[技术架构详解]
* link:DOCKER-README.md[Docker 部署指南]
* link:TEST-README.md[测试说明]

== License

MIT License

